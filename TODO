TODO
====

* create and use python virtual environment for maposmatic frontend and render backend

* more integration tests:
 * test API
 * test Umgebnugsplaene

* use "cartocc" npm for modifying styles instead of sed

* create OpenRiverboatMap indexes

* populate the weblate instance

* make weblate run as a subdir of main apache instead of on separate port
